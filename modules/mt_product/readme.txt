#INSTRUCTION FOR TESTING.

Before install mt_product module you should:
  1 - Install video_embed_field module
  2 - Install paragraphs module

When the module is installed it creating:
 Product content type with required fields,
 3 Paragraph types with required fields,
 Taxonomy - Product Categories.

